"use strict";
// объект screen показывает размеры(свойства) монитора
// console.log(screen);

// console.dir(document);

const hight = document.documentElement.clientHeight;
console.log("hight:", hight);
const width = document.documentElement.clientWidth;
console.log("width:", width);

//Еще свойства offsetHeight, offsetWidth, scrollHeight, scrollWidth,scrollTop, scrollLeft, scrollBy, scrollTo, получить координаты элемента - getBoundingClientRect
